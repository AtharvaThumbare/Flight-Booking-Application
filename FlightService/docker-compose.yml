services:

  flight-db:
    image: postgres:14

    container_name: flight-db

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: flight_serviceDB

    ports:
      - "5433:5432"

    volumes:
      - ./Docker/postgres_data:/var/lib/postgresql/data